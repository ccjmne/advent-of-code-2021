import solution from 'src/solution-module'

export default solution({
  parse(data: string) {
    return data
      .trim()
      .split(/\n/g)
  },

  partI(input) {
    // TODO: impl
  },

  partII(input) {
    // TODO: impl
  },
})
